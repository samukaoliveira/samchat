<%= form_with(model: sala_bate_papo) do |form| %>
  <% if sala_bate_papo.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sala_bate_papo.errors.count, "error") %> prohibited this sala_bate_papo from being saved:</h2>

      <ul>
        <% sala_bate_papo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :topico, style: "display: block" %>
    <%= form.text_field :topico %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
